{
  "name": "nemoclaw",
  "version": "0.1.0",
  "description": "NemoClaw — run OpenClaw inside OpenShell with NVIDIA inference",
  "license": "Apache-2.0",
  "bin": {
    "nemoclaw": "./bin/nemoclaw.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:all": "vitest run",
    "test:integration": "vitest run --testPathPattern=integration",
    "test:serial": "vitest run --pool=forks --poolOptions.forks.singleFork",
    "test:timing": "vitest run --reporter=verbose",
    "test:coverage": "vitest run --coverage",
    "changelog": "git log --oneline --no-merges $(git describe --tags --abbrev=0 2>/dev/null || git rev-list --max-parents=0 HEAD)..HEAD 2>/dev/null || echo 'No tags found — showing full log:' && git log --oneline --no-merges | head -50",
    "duplicates": "npx --yes jscpd --min-lines 10 --min-tokens 50 --ignore 'node_modules/**,dist/**,nemoclaw/dist/**' . 2>/dev/null || echo 'jscpd analysis complete'",
    "tech-debt": "grep -rn 'TODO\\|FIXME\\|HACK\\|XXX' --include='*.js' --include='*.ts' --include='*.py' . 2>/dev/null | grep -v node_modules | grep -v dist || echo 'No technical debt markers found'",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "typecheck": "tsc -p jsconfig.json",
    "typecheck:cli": "tsc -p tsconfig.cli.json",
    "prepare": "npm install --omit=dev --ignore-scripts 2>/dev/null || true && if [ -d .git ]; then if command -v prek >/dev/null 2>&1; then prek install; elif [ -d node_modules/@j178/prek ]; then echo \"ERROR: prek package found but binary not in PATH\" && exit 1; else echo \"Skipping git hook setup (prek not installed)\"; fi; fi",
    "prepublishOnly": "cd nemoclaw && env -u npm_config_global -u npm_config_prefix -u npm_config_omit npm install --ignore-scripts && ./node_modules/.bin/tsc"
  },
  "dependencies": {
    "@sentry/node": "^10.47.0",
    "openclaw": "2026.3.11",
    "p-retry": "^4.6.2",
    "uuid": "^13.0.0"
  },
  "bundleDependencies": [
    "p-retry"
  ],
  "files": [
    "bin/",
    "nemoclaw/dist/",
    "nemoclaw/openclaw.plugin.json",
    "nemoclaw/package.json",
    "nemoclaw-blueprint/",
    "scripts/",
    "Dockerfile",
    ".dockerignore"
  ],
  "engines": {
    "node": ">=22.16.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/NVIDIA/NemoClaw.git"
  },
  "devDependencies": {
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "@eslint/js": "^10.0.1",
    "@j178/prek": "^0.3.6",
    "@types/node": "^25.5.0",
    "@vitest/coverage-v8": "^4.1.2",
    "eslint": "^10.1.0",
    "execa": "^9.6.1",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
